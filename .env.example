# QuotaMonitor 配置示例
#
# 复制本文件为 .env，按需修改后通过"设置面板"填入。
# 注意：API Key 不会读取本文件，必须通过 UI 输入（存 macOS Keychain），
#       本文件只配置可选的远程服务。

# Bark 推送服务器（可选）
# 默认官方：https://api.day.app
# 自建示例：https://bark.your-domain.com
BARK_SERVER=https://api.day.app
